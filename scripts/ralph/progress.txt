## Codebase Patterns
- Scripts run from `$CONFIG_DIRECTORY/scripts` (set by BlueBuild) — use `${CONFIG_DIRECTORY}/` to reference repo-root files from scripts
- `set dotenv-filename := "xfce-winxp-tc.env"` in justfile loads dotenv from the justfile's directory upward (Just traverses up the tree)
- When justfile uses dotenv-loaded vars in `#!/bin/bash` recipe blocks, use `$VARNAME` (shell env), NOT `{{ VARNAME }}` (Just variable)
- Use `# shellcheck disable=SC1091` for sourced files with dynamic paths; use `# shellcheck disable=SC2046` when intentional word-splitting for package lists
- shellcheck severity: SC1091 is "info" level — `--severity=warning` passes even with this
- GitHub Actions workflow for RPM builds: use `docker` (not `podman`) on ubuntu-latest; use `--volume "$PWD:/app:Z"` for SELinux-compat label
- oras push syntax: `oras push <ref> --artifact-type <type> .` (run from within the directory of files to push)
- `workflow_call` trigger enables calling build-packages.yml from other workflows (needed for US-006)
- oras pull: `oras pull <ref>` downloads artifact files into the current directory; use `mktemp -d` + `cd` pattern for clean temp dir

# Ralph Progress Log
Started: Wed Mar 18 01:04:45 PM CDT 2026
---

## 2026-03-18 - US-001
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created `xfce-winxp-tc.env` at repo root with `XFCE_WINXP_TC_VERSION=4f73e4a740041635eecd30abccc2fdafabb21582`
- Updated `files/scripts/20-xfce-winxp-tc.sh` to source env file via `${CONFIG_DIRECTORY}/xfce-winxp-tc.env` instead of hard-coding the hash
- Updated `files/scripts/packages/xfce-winxp-tc/justfile` to use `set dotenv-filename := "xfce-winxp-tc.env"` and `$XFCE_WINXP_TC_VERSION` (shell) instead of `{{ XFCE_WINXP_TC_VERSION }}` (Just variable)
- Files changed: `xfce-winxp-tc.env` (new), `files/scripts/20-xfce-winxp-tc.sh`, `files/scripts/packages/xfce-winxp-tc/justfile`
- **Learnings for future iterations:**
  - BlueBuild's `$CONFIG_DIRECTORY` is set to the repo root in the build environment; scripts can source files relative to it
  - Just's `dotenv-filename` directive searches for the named file starting from the justfile's directory upward — env file at repo root is found when running `just` from the `files/scripts/packages/xfce-winxp-tc/` directory
  - SC1091 (not following source) is info-level in shellcheck — suppress with `# shellcheck disable=SC1091` for dynamic paths
  - SC2046 (unquoted command substitution) is intentional for package lists — suppress with `# shellcheck disable=SC2046`
---

## 2026-03-18 - US-002
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Created `.github/workflows/build-packages.yml` with workflow triggers: `workflow_dispatch`, `workflow_call`, push/PR on `xfce-winxp-tc.env` or the workflow file itself
- Workflow reads `XFCE_WINXP_TC_VERSION` from `xfce-winxp-tc.env` via `source` and exports to `GITHUB_OUTPUT` and `GITHUB_ENV`
- Sets artifact tag to `pr-<number>` on PR events, otherwise uses the commit hash from the env file
- Clones xfce-winxp-tc at the specific version, builds dev image from existing Containerfile, runs `bash /app/packaging/buildall.sh` inside the container
- Collects RPMs from `xptc/*/rpm/std/x86_64/fre/`, fails if directory is empty
- Installs oras v1.2.0, logs in to GHCR, pushes RPMs as OCI artifact to `ghcr.io/winblues/xfce-winxp-tc-rpms:<tag>`
- Files changed: `.github/workflows/build-packages.yml` (new)
- **Learnings for future iterations:**
  - On ubuntu-latest, use `docker` (not `podman`) for container builds — podman requires extra setup
  - The `--volume "$PWD:/app:Z"` flag with `:Z` is needed for SELinux labeling compatibility
  - `workflow_call` trigger (no inputs needed here) allows this workflow to be called from build.yml for US-006
  - oras push directory: run `oras push <ref> --artifact-type <type> .` from inside the RPM directory
  - RPMs land in `xptc/*/rpm/std/x86_64/fre/` inside the cloned source tree
---

## 2026-03-18 - US-003
Thread: https://ampcode.com/threads/$AMP_CURRENT_THREAD_ID
- Rewrote `files/scripts/20-xfce-winxp-tc.sh` to pull pre-built RPMs from GHCR instead of compiling from source
- Script now: installs `oras-cli` via `dnf5`, creates a temp dir, runs `oras pull ghcr.io/winblues/xfce-winxp-tc-rpms:${XFCE_WINXP_TC_VERSION}`, installs RPMs with `rpm-ostree install ./*.rpm`, sets plymouth theme
- Removed: build deps install, git clone, git checkout, bash packaging/buildall.sh
- `set -xueo pipefail` ensures `oras pull` failure exits non-zero immediately
- Files changed: `files/scripts/20-xfce-winxp-tc.sh`
- **Learnings for future iterations:**
  - `oras pull <ref>` downloads artifact files into the current directory — use `mktemp -d && cd` for isolation
  - `./*.rpm` glob passed directly to `rpm-ostree install` is clean and shellcheck-safe
  - `set -e` (via `set -xueo pipefail`) guarantees non-zero exit if any step fails, including oras pull
  - `xfce-winxp-tc.env` at repo root remains accessible via `${CONFIG_DIRECTORY}/xfce-winxp-tc.env` — no need to move it
---
